#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s;
    cin >> s;

    string x;

    x.assign(s);

    // cout << x.length();

    // cout << x.substr(2, 5);

    cout << x.find('l');
    return 0;
}